// Write a program to print the ASCII value of a number.
#include <stdio.h>
int main()
{
    char a;
    printf("enter the character : ");
    scanf("%c", &a);
    printf("the ASCII value  is %d", a);
}